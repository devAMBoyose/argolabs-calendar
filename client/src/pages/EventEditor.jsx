import React from 'react';
import { useEffect,useState } from 'react';import { useNavigate,useParams } from 'react-router-dom';import api from '../api';import { useAuth } from '../context/AuthContext';
const initial={title:'',description:'',department:'HR',location:'',startAt:'',endAt:'',attendees:'',priority:'NORMAL',status:'PENDING',remarks:'',isPublic:true,reminderMinutes:30};
export default function EventEditor(){const {id}=useParams();const nav=useNavigate();const {isOwner,canEdit}=useAuth();const [form,setForm]=useState(initial);const [saving,setSaving]=useState(false);const [error,setError]=useState('');useEffect(()=>{if(id)api.get(`/events/${id}`).then(({data})=>setForm({...data.event,attendees:(data.event.attendees||[]).join(','),startAt:data.event.startAt?.slice(0,16),endAt:data.event.endAt?.slice(0,16)}))},[id]);const set=(k,v)=>setForm(f=>({...f,[k]:v}));async function save(e){e.preventDefault();setSaving(true);setError('');try{const payload={...form,attendees:form.attendees.split(',').map(x=>x.trim()).filter(Boolean)};if(id)await api.patch(`/events/${id}`,payload);else await api.post('/events',payload);nav('/dashboard')}catch(err){setError(err.response?.data?.message||'Unable to save event.')}finally{setSaving(false)}}async function remove(){if(!confirm('Delete this event from the portal and Google Calendar?'))return;await api.delete(`/events/${id}`);nav('/dashboard')}return <><div className="page-header"><div><span className="eyebrow">{id?'EVENT RECORD':'NEW EVENT'}</span><h1>{id?'Update event':'Create event'}</h1><p>{isOwner?'You can manage all event details.':'You can update only status and remarks.'}</p></div></div><form className="form-card" onSubmit={save}>{error&&<div className="alert error">{error}</div>}<div className="form-grid"><label>Event title<input required disabled={!isOwner} value={form.title} onChange={e=>set('title',e.target.value)}/></label><label>Department<select disabled={!isOwner} value={form.department} onChange={e=>set('department',e.target.value)}>{['HR','Finance and Accounting','Operations','Sales and Marketing','Warehouse','Management','IT and Systems','General'].map(x=><option key={x}>{x}</option>)}</select></label><label>Start<input type="datetime-local" required disabled={!isOwner} value={form.startAt} onChange={e=>set('startAt',e.target.value)}/></label><label>End<input type="datetime-local" required disabled={!isOwner} value={form.endAt} onChange={e=>set('endAt',e.target.value)}/></label><label>Location<input disabled={!isOwner} value={form.location||''} onChange={e=>set('location',e.target.value)}/></label><label>Priority<select disabled={!isOwner} value={form.priority} onChange={e=>set('priority',e.target.value)}>{['NORMAL','HIGH','URGENT'].map(x=><option key={x}>{x}</option>)}</select></label><label className="full">Guest emails<textarea disabled={!isOwner} value={form.attendees} onChange={e=>set('attendees',e.target.value)} placeholder="name@company.com, another@company.com"/></label><label className="full">Description<textarea disabled={!isOwner} value={form.description||''} onChange={e=>set('description',e.target.value)}/></label><label>Status<select disabled={!canEdit} value={form.status} onChange={e=>set('status',e.target.value)}>{['PENDING','ONGOING','COMPLETED','CANCELLED','TERMINATED'].map(x=><option key={x}>{x}</option>)}</select></label><label>Reminder<select disabled={!isOwner} value={form.reminderMinutes} onChange={e=>set('reminderMinutes',Number(e.target.value))}>{[10,15,30,60,1440].map(x=><option key={x} value={x}>{x===1440?'1 day':`${x} minutes`}</option>)}</select></label><label className="full">Remarks<textarea disabled={!canEdit} value={form.remarks||''} onChange={e=>set('remarks',e.target.value)} placeholder="Operational update, completion note, reason for cancellation…"/></label>{isOwner&&<label className="checkbox"><input type="checkbox" checked={form.isPublic} onChange={e=>set('isPublic',e.target.checked)}/>Publish on public calendar</label>}</div><div className="form-actions"><button type="button" className="ghost" onClick={()=>nav('/dashboard')}>Cancel</button>{isOwner&&id&&<button type="button" className="danger" onClick={remove}>Delete</button>}<button className="primary" disabled={saving}>{saving?'Saving…':'Save event'}</button></div></form></>}
