import crypto from 'crypto';function key(){const raw=process.env.GOOGLE_OAUTH_ENCRYPTION_KEY||'';if(!/^[a-f0-9]{64}$/i.test(raw))throw new Error('GOOGLE_OAUTH_ENCRYPTION_KEY must be 64 hexadecimal characters');return Buffer.from(raw,'hex')}export function encrypt(text){const iv=crypto.randomBytes(12);const cipher=crypto.createCipheriv('aes-256-gcm',key(),iv);const enc=Buffer.concat([cipher.update(text,'utf8'),cipher.final()]);const tag=cipher.getAuthTag();return [iv,tag,enc].map(x=>x.toString('base64url')).join('.')}export function decrypt(payload){const [iv,tag,enc]=payload.split('.').map(x=>Buffer.from(x,'base64url'));const d=crypto.createDecipheriv('aes-256-gcm',key(),iv);d.setAuthTag(tag);return Buffer.concat([d.update(enc),d.final()]).toString('utf8')}
