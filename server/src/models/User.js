import mongoose from 'mongoose';import bcrypt from 'bcryptjs';const schema=new mongoose.Schema({name:{type:String,required:true,trim:true},email:{type:String,required:true,unique:true,lowercase:true,trim:true},passwordHash:{type:String,required:true,select:false},role:{type:String,enum:['OWNER','EDITOR','VIEWER'],default:'VIEWER'},isActive:{type:Boolean,default:true}},{timestamps:true});schema.methods.verifyPassword=function(p){return bcrypt.compare(p,this.passwordHash)};schema.statics.hashPassword=(p)=>bcrypt.hash(p,12);export default mongoose.model('User',schema);
