import {Router} from 'express';import {requireAuth,allowRoles} from '../middleware/auth.js';import {list,one,create,update,remove} from '../controllers/eventController.js';const r=Router();r.use(requireAuth);r.get('/',list);r.get('/:id',one);r.post('/',allowRoles('OWNER'),create);r.patch('/:id',allowRoles('OWNER','EDITOR'),update);r.delete('/:id',allowRoles('OWNER'),remove);export default r;
